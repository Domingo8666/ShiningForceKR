# Shining Force Gaiden: Final Conflict 한국어 패치 인수인계

## 대상과 저장 위치

- 플랫폼: Sega Game Gear
- 게임: Shining Force Gaiden: Final Conflict (Japan)
- 메가 CD판이 아닙니다.
- 기준 저장소: https://github.com/Domingo8666/ShiningForceKR
- ROM 작업 위치: S25U 내부 저장공간/ShiningForceKR
- PC는 프로젝트 자료 저장소로 사용하지 않습니다.

## 기준 자료

- 깨끗한 일본판 ROM: S25U 내부 저장공간/ROM 로컬 전용
- 진단 기준 패치: patch/Final_Conflict_Japan_to_Korean_v5.1.bps
- 외부 영어 참조: fcpatch_070706.ips, S25U 로컬 전용
- 압축 조사: analysis/compression_research.md
- 패치 구조: analysis/v5_1_patch_layout.md
- 문제 목록: docs/issues.md

v5.1은 최종판이 아니라 재작업용 진단 기준입니다.

## 기존 범위 기록

기존 보고서는 전체 1,492개, 번역 처리 103개, 미처리 1,389개, 위험 미처리 1,297개, 한글 글리프 168개라고 기록합니다. 단어 사전 확장이 검증되지 않은 결과이므로 완료율 근거로 쓰지 않습니다.

## 2026-07-29 완료 항목

1. BPS 크기, SHA-256과 소스/대상/패치 CRC 검증
2. BPS 액션 3,305개와 ROM 확장 구조 기록
3. 영어 IPS 크기, SHA-256과 288개 레코드 검증
4. Huffman 벡터 0x29C3F, 256개 엔트리와 221개 트리 확인
5. 독립 BPS/IPS/Huffman Python 파서와 합성 테스트 추가
6. ROM과 외부 IPS가 Git에 올라가지 않도록 규칙 강화
7. 한국어 v5.1 벡터를 0x80100에서 확인하고 51개 트리 전부 파싱
8. 한국어 심벌/트리 구간 0x80300..0x808D3 확정
9. 244엔트리 글꼴 페이지 매핑과 뱅크 0x22..0x5E 배치 확인
10. S25U 원클릭 검증 파이프라인과 실제 BPS 회귀 테스트 추가
11. 전체 v5.1 ROM의 코드 리터럴 및 2·3바이트 조회표 후보 자동 점수화 추가
12. 내 파일 앱에서 바로 보이는 reports/NEXT_STEP.txt 생성 경로 추가
13. S25U 첫 탐색에서 3바이트 110개, 2바이트 50개와 최상위 0x000B7D 관측
14. 상위 후보의 Z80 slot 가설과 emucap read breakpoint/매퍼 스냅샷 계획 자동 생성
15. 0x010000 후보를 과대평가한 0x4000/0x8000 제어 흐름 오탐을 격리하고 점수식 v2로 수정
16. 뱅크 리터럴 단독 출현과 슬롯별 Sega mapper 쓰기를 분리하고 점수식 v3 회귀 테스트 추가
17. ROM·대사·로컬 경로를 제외한 S25U 관측 요약의 GitHub 자동 게시 경로 추가
18. 0x000B7A/0x000B7B 시프트 해석을 정렬 클러스터로 묶고 0x000B7A..0x000C2E 통합 감시 범위 생성
19. 런타임 관측·진단·소비자 해석을 검증한 뒤 허용된 파일만 한 커밋으로 게시하는 S25U 묶음 게시 단계 추가
20. 표 범위 히트와 실제 압축 대상 주소 히트를 분리하고, 두 번째 히트의 논리 주소와 매퍼 뱅크가 모두 맞을 때만 정렬을 확정하도록 승격 조건 강화
21. ROM 없이 `한다`의 페이지 27 글리프와 `5F 03 0D 1F 04 C9` Huffman 39비트 왕복을 검증하는 fail-closed 사전검증 추가
22. schema v2 런타임 엔트리가 확정될 때만 깨끗한 원본에서 시험 ROM·로컬 IPS를 만드는 Expected Write/diff 감사 빌더 추가
23. S25U 첫 런타임 범위 히트가 슬롯 2의 실제 뱅크 2에서 발생해 물리 뱅크 0 조회표 증거가 아님을 확인하고, 매퍼 뱅크 불일치 히트를 거부한 뒤 탐색을 계속하도록 수정
24. 뱅크 불일치 오탐 제거 뒤 물리 0x000B7A..0x000C2E 후보가 1,680프레임 입력 구간에서 읽히지 않음을 확인하고, 직전 무히트 범위를 제외한 다음 두 bounded-decode 3바이트 후보군을 자동 순회하도록 확장
25. 물리 뱅크 0·6·16의 상위 3바이트 후보군 8개 논리 범위가 모두 무히트임을 확인하고, 최근 Git 이력의 음성 증거를 누적한 뒤 검증된 렌더러 호출 좌표 0x003FD5·0x03FFB2를 execute breakpoint로 역추적하는 단계 추가
26. 기존 렌더러 추적이 Start 입력으로 도입 장면을 건너뛴 경로 오류를 확인하고, 콜드 부팅 한 번에 여섯 후보를 동시에 감시하며 12,000프레임 동안 아무 입력 없이 attract intro를 기다리도록 수정
27. S25U 무입력 attract intro에서 0x5F 전용 페이지 렌더러 호출이 0회임을 확인하고, 더 앞선 공용 텍스트 디코더 0x003411의 세 매핑을 감시한 뒤 첫 히트부터 slot 1·2 ROM read를 최대 64개까지 매퍼 일치 물리 좌표로 수집하도록 전환
28. S25U에서 공용 텍스트 디코더도 12,000프레임 무입력 attract 경로에 나타나지 않음을 확인하고, 취소인 1번 버튼을 제거한 뒤 Start와 2번 확인 버튼으로 새 게임 스토리를 진행하는 경로로 교정
29. Start·2번 확인 경로 3,300프레임을 요청한 초기 무히트 결과를 기록하고, 검증되지 않은 0x003411 진입점 대신 물리 0x080100..0x0802FF 한국어 Huffman 벡터의 slot 1·2, mapper bank 0x20 실제 read를 직접 추적하도록 전환
30. 같은 요청 경로에서 Huffman 벡터 read가 없을 때 부팅·Start·확인 1/4/16회 화면 5개를 S25U에만 저장하고, GitHub에는 PNG 해시와 정지 판정만 게시하는 경로 진단을 추가
31. Gearsystem 3.9.14의 `debug_step_frame`가 완료가 아니라 요청 접수 직후 응답함을 소스와 단색 캡처로 확인했습니다. 초기 3,300프레임 무히트·화면 해시 결론을 폐기하고, `debug_get_status.paused`/breakpoint 완료 장벽과 새 증거 스키마로 전체 런타임 경로를 재검증하도록 수정했습니다.
32. 동기화된 첫 slot 0·bank 0 hit가 감시 범위 내부 PC 0x0B7B의 opcode fetch임을 확인했습니다. 실행 물리 PC가 감시 물리 범위와 겹치는 hit를 데이터 read로 승격하지 않고 해당 매핑을 제외한 다음 매핑을 계속 추적하도록 수정했습니다.
33. USB ADB로 최초 검증한 뒤 케이블 없이도 새 `origin/main` 커밋만 한 번씩 처리하는 S25U 자동실행기를 추가했습니다. canonical main·ROM 공유 저장공간·safe artifact allowlist를 벗어나거나 안전하지 않은 로컬 변경·커밋을 발견하면 중단하고, 동일 실패 커밋을 반복 실행하지 않습니다.
34. S25U 자동실행의 설치·시작·상태·로그·안전 중지를 한 관리 도구로 묶었습니다. 죽은 PID의 stale lock만 복구하고 살아 있는 프로세스와 경쟁하지 않으며, 내 파일 앱에서 확인할 `reports/AUTOPILOT_STATUS.txt`와 Termux:Boot 개인 실행기를 생성합니다.
35. 자동실행 첫 작업이 Gearsystem 준비·대상 식별을 모두 통과한 뒤 `runtime-command`에서 종료 코드 1을 게시했습니다. 경로·stderr를 공유하지 않고 실패 단계·종류·마지막 MCP 메서드만 schema v3 진단에 포함하도록 런타임 probe의 실패 영수증을 추가했습니다. 새 소스 커밋은 같은 S25U 자동실행에서 원인 분류를 다시 수집합니다.
36. 재수집 결과 실제 실패가 `candidate-probe/frame-step-timeout`임을 확인했습니다. proot의 S25U 실행을 PAL 실시간 속도로 가정한 8.6초 제한을 폐기하고, 180프레임은 최소 60초·240프레임은 63초를 허용하는 bounded 완료 장벽으로 수정했습니다.
37. 60초 제한에서도 같은 실패가 재현되어 Gearsystem 3.9.14 소스를 대조했습니다. 장거리 경로 전체의 CPU trace가 모든 Z80 명령을 기록하는 비용을 원인 경계로 보고, 첫 단계는 trace 없이 mapper 일치·실행 범위 비중첩 read hit만 수집하며 정확한 접근 명령은 확정 구간의 짧은 후속 replay로 분리했습니다.
38. CPU trace를 꺼도 60초 multi-frame 완료가 돌아오지 않아 Gearsystem의 pending frame·pause 구현을 추가 확인했습니다. 브레이크포인트 감시 중에는 취소할 수 없는 multi-frame 예약을 쓰지 않고, 1프레임마다 `debug_get_status.paused` 또는 breakpoint를 확인하는 정확하고 bounded한 경로로 교체했습니다.
39. S25U가 단일 프레임 장벽으로 3,300프레임 경로를 정상 완료해 시간초과를 해소했습니다. 현재 입력 경로의 상위 조회표 후보는 매퍼 일치 read가 없었으므로 반복하지 않고, BPS만으로 확인되는 0x0033FA..0x003405 패치 코드와 0x003432의 tree bank 0x20 리터럴을 근거로 0x33FA·0x3411·0x3431 execute 후보를 먼저 추적하도록 전환했습니다.
40. Start·2번 확인 3,300프레임에서 세 execute 후보가 모두 무히트임을 동기화된 S25U 실행으로 확인했습니다. 오프닝 소개가 무입력 대기에서 시작된다는 게임 진행 자료에 맞춰, 다음 실행은 동기화된 무입력 12,000프레임을 먼저 검사한 뒤 2번 확인과 1번 확인을 각각 별도 콜드 부팅으로 비교합니다.
41. 무입력·버튼 경로 매트릭스 첫 실행의 `candidate-probe/invalid-runtime-data`를 분석해, 단일 호출의 12,000프레임이 안전 상한 1,000을 넘긴 것이 원인임을 확인했습니다. 무입력 총예산은 유지하되 1,000프레임 12구간으로 나눴고, renderer 단계의 schema v3 실패 영수증도 유지합니다.
42. 동기화된 무입력 attract intro에서 물리 0x0033FA 디코더 execute hit를 확정했습니다. 진입 시 slot 1은 bank 8이었고 PC 0x3406의 source-region read 64개를 수집했으나 Huffman bank 0x20 전환 전 표본 한도에 도달했습니다. 다음 실행은 source-region 표본을 8개만 보존하면서 최대 1,024개 read를 따라가 한국어 벡터·트리 표본을 우선 수집합니다.
43. 같은 attract intro를 최대 1,024개 read로 연장해 mapper bank 0x20의 한국어 Huffman vector 3개와 tree 16개를 실제로 확인했습니다. 다음 실행은 원시 opcode를 게시하지 않고 각 read의 고정 operand-kind만 기록해 stream·vector·tree 경계를 연결합니다.
44. 실제 인트로 source-region 시작점 0x0203DE·0x0203E8·0x0203FB·0x02041E가 BPS만으로 종료 심벌까지 정확히 디코드·재인코딩되고 서로 겹치지 않음을 확인했습니다. 첫 54비트 스트림은 39비트 시험 문장 `한다`를 수용하므로 새 안전 해석기가 조회표 추정 없이 이를 S25U 시험 빌더와 attract 화면 캡처에 전달합니다.
45. S25U가 `한다` 시험 ROM과 IPS를 로컬에 생성했고 기준/시험 SHA-256도 기록했습니다. 첫 캡처는 같은 논리 주소의 mapper bank 7 선행 오탐에서 멈췄으므로, 후속 실행은 bank 8과 디코더 PC가 함께 일치할 때까지 breakpoint를 한 명령씩 안전하게 넘겨 재시도합니다.
46. bank 8·0x0203DE 시험 캡처 5장을 사람이 확인했으나 `한다`가 어느 프레임에도 나타나지 않았습니다. 초상·대화창·주변 한국어와 입력 뒤 화면 전이는 유지됐으므로 성공으로 승격하지 않고 캡처 해시와 불리언 판정만 안전 검토 기록에 남겼습니다. 같은 스트림을 반복하지 않고 다음 실제 디코더 스트림 0x0203E8을 시험합니다.
47. bank 8·0x0203E8의 두 번째 캡처도 같은 화면만 보이고 `한다`가 나타나지 않아 기각했습니다. 남은 관측 스트림 0x0203FB·0x02041E는 각각 38·27비트로 39비트 시험 문장을 수용할 수 없습니다. 두 실패 주소를 누적 제외하고, 다음 S25U 실행은 Huffman 표본 16개를 보존하면서 source-region 표본을 8개에서 32개로 늘려 뒤쪽 실제 디코더 스트림을 수집합니다.
48. 확대 수집으로 실제 디코더 스트림을 18개까지 확보했고 bank 8·0x020447의 57비트 스트림을 시험했으나 `한다`가 나타나지 않아 세 번째로 기각했습니다. 첨부된 주 화면에서는 초상·대화창·주변 한국어가 유지됐지만 진행 후 화면은 확인하지 못해 해당 관측만 미확인으로 기록했습니다. 다음 왕복 정확 후보는 97비트의 0x020462입니다.
49. bank 8·0x020462의 세 화면에서도 기존 대사만 표시되고 `한다`가 나타나지 않아 네 번째로 기각했습니다. 초상·대화창·주변 한국어는 유지됐고 진행 후 PNG는 이전에 확인한 정상 해시와 동일했습니다. 다음 왕복 정확 후보는 시험 문장 39비트를 수용하는 41비트 스트림 0x020473입니다.
50. bank 8·0x020473의 세 첨부 화면에서도 기존 대사만 표시되고 `한다`가 나타나지 않아 다섯 번째로 기각했습니다. 초상·대화창·주변 한국어는 유지됐고 진행 후 PNG는 직전 사람이 확인한 정상 해시와 동일했습니다. 다음 왕복 정확 후보는 87비트 스트림 0x0204B1입니다.
51. 기준 ROM과 시험 ROM을 같은 read 지점에서 각각 캡처해 정규화 RGBA 픽셀을 비교하는 자동 판정을 추가했습니다. 모든 캡처가 0픽셀 변화일 때만 후보를 자동 탈락시키고, 변화가 있으면 사람 검토에서 멈춥니다. 한 장치 작업에서 최대 8개 후보를 연속 처리하며 GitHub 확인 기본 주기를 300초에서 60초로 줄였습니다.
52. 첫 자동 비교 실행은 준비 검사 전체를 통과했지만 종료 코드 1만 남기고 정확한 하위 단계를 구분하지 못했습니다. 확정 디코더 stream과 로컬 기준 ROM SHA-256이 일치하면 오래 걸리는 초기 탐색을 반복하지 않고 곧바로 시험 빌드·기준/시험 화면 비교로 이동하도록 수정했으며, 이후 실패는 시험 빌드·캡처·비교 등 정확한 안전 단계 토큰으로 기록합니다.
53. USB ADB로 S25U 로컬 로그와 실제 PNG를 확인해 자동 비교 실패가 `display-pixel-comparison/invalid-runtime-data`이며, Gearsystem 캡처가 유효한 IEND 뒤에 2바이트 NUL 패딩을 붙이는 것이 원인임을 확정했습니다. 청크 CRC·IEND·이미지 구조는 그대로 전수 검증하면서 IEND 뒤 최대 16바이트의 0 패딩만 허용하고, 비영·과도한 후행 데이터는 계속 거부하도록 수정했습니다.
54. 0x0204B1 시험에서 8·30·90프레임 화면에 40·165·459픽셀 변화가 검출되고 진행 후 화면은 기준과 같아 사람 검토 게이트가 열렸습니다. 다음 자동실행은 변경 픽셀이 가장 많은 기준·시험 화면과 진행 후 화면을 해시 검증해 `reports/HUMAN_REVIEW/`의 쉬운 이름으로 복사합니다. 사용자는 복잡한 evidence 경로를 찾지 않고 PNG 3개만 확인할 수 있습니다.
55. 패치된 디코더의 0x33FA 코드가 `0x3FE8 + DE`에서 16비트 블록 시작 포인터를 읽는 구조를 검산했습니다. 다음 S25U 캡처는 목표 stream read 순간의 DE를 다시 수집하고, 기준 ROM의 현재·다음 포인터가 목표 0x44B1을 실제로 둘러싸는 경우에만 블록 엔트리 번호·블록 내 오프셋을 안전 요약에 게시합니다.
56. 첫 엔트리 연결 실행은 기준·시험 target hit의 DE가 정적 기대와 다를 때 전체 캡처 게시까지 중단하는 과도한 게이트로 종료됐습니다. DE 불일치·범위 밖 값은 엔트리 확정 실패로만 기록하고 이미 검증된 화면 비교는 보존하도록 바꿨으며, 현재·다음 포인터가 실제로 목표를 둘러싸는 경우에만 `resolved`로 승격합니다.
57. target read 순간의 DE가 기준 12·시험 234로 갈라져 원래 엔트리 선택자가 아니라 디코딩 중 임시 상태임을 확인했습니다. 다음 S25U 실행은 0x33FA 디코더 진입점에서 DE를 별도로 보존하고, 정적 포인터 범위가 요구하는 선택자 2가 확인된 호출 뒤의 0x44B1 read만 승인합니다.
58. S25U가 기준·시험 모두 0x33FA 진입 DE=2를 확인해 상위 그룹 1, 시작 0x43DE, 목표 그룹 내 바이트 오프셋 0xD3을 확정했습니다. 다음 실행은 같은 진입점의 B 상위 바이트를 기준·시험에서 함께 보존해, 연속 Huffman 그룹 안에서 실제 선택된 엔트리 서수를 연결합니다.
59. 기준·시험 0x33FA 진입의 B가 모두 147임을 확인했습니다. Final Conflict 그룹은 엔트리 사이를 바이트 정렬하지 않으므로, B+1개 종료 심벌을 같은 비트 커서로 연속 디코드·재인코드하고 148번째 엔트리의 정확한 비트 경계가 목표 0x44B1 read를 포함하는지 S25U 전체 ROM으로 검증합니다.
60. 0x43DE부터 148개 엔트리를 바이트 패딩 없이 연속 디코드·재인코드한 결과는 정확히 일치했고, 148번째 엔트리의 바이트 범위는 0x49E0..0x49EE였습니다. 0x44B1은 B=147이 고른 최종 대사의 시작이 아니라 앞선 147개 엔트리를 건너뛰는 동안 읽힌 중간 바이트입니다. 따라서 B의 엔트리 서수 의미는 유지하고 시험 패치의 목표를 실제 148번째 엔트리로 교정합니다.
61. 기존 `한다` 시험 문구의 페이지 27·심벌 0x1F 타일이 `한`이 아니라는 사실을 Galmuri7 BDF 픽셀 대조로 발견했습니다. 정확한 `한`은 페이지 6·0x11, `다`는 같은 페이지의 0x04이며, 표시 심벌열을 `5F 02 08 11 04`, 종료 포함 Huffman 입력을 31비트 `EA 51 2D 10`으로 교정했습니다.
62. Galmuri7 BDF를 고정 커밋·SHA-256으로 받는 도구와 전체 v5.1 글꼴 카탈로그를 추가했습니다. source-independent 타일 7,564개 중 5,939개가 한글과 유일하게 픽셀 일치하고 53개는 복수 후보, 1,572개는 미일치이며, 유일 일치한 서로 다른 한글은 401자입니다. 전체 좌표·후보·타일 해시는 `analysis/local/`에만 생성합니다.
63. 실제 선택 엔트리는 물리 0x0209E0의 bit 7에서 시작하는 106비트 문자열로 확정했습니다. 기술 시험 빌더는 이제 0x0204B1의 바이트 시작을 덮지 않고 이 비트 경계부터 교정한 31비트 `한다`만 Expected Write로 넣으며, 첫·마지막 바이트의 이웃 비트는 보존합니다. 원래 꼬리는 조기 종료 심벌 뒤에 그대로 두는 화면 증명 전용 정책이므로 최종 배포 패치로 승격하지 않습니다.
64. S25U schema v5가 0x44B1을 포함하는 실제 prefix 엔트리를 0부터 센 22번, bit 1550..1750, 57심벌·200비트로 독립 계산했습니다. 이는 B=147이 고른 최종 엔트리와 다르며, 0x44B1 변경 화면이 선택 대사 교체 증거가 아니었다는 설명을 확정합니다.
65. 최종 배포 산출물용 결정적 BPS 생성기를 추가했습니다. 같은 좌표의 동일 바이트는 SourceRead, 변경·확장 구간은 TargetRead만 사용해 감사하기 쉬운 패치를 만들고, 소스·대상·패치 CRC와 메타데이터를 검사한 뒤 새 패치를 소스에 독립 재적용한 결과가 대상과 완전히 같을 때만 반환합니다.
66. 실제 선택 엔트리 코드를 올린 첫 자동실행은 기존 decoder stream 준비 판정이 false일 때 시험 빌드 루프를 성공 코드로 조용히 건너뛰어 이전 화면 아티팩트가 그대로 남았습니다. baseline SHA-256과 검증된 schema v5 그룹 prefix가 맞으면 별도 `group_selection_ready` 게이트로 빌드·캡처를 반드시 실행하도록 수정했습니다.
67. 선택 엔트리의 106비트 전체 길이를 보존하는 `한다` 인코딩을 추가했습니다. 출력 없는 페이지 선택 제어만 반복하고 실제 글리프 전과 종료 전 페이지를 6으로 맞춘 심벌열은 정확히 106비트 `EA4A95D4A95D4A95D512D32D4400`으로 왕복합니다. 따라서 엔트리 뒤 비트를 이동하지 않고 조기 종료 뒤 원래 꼬리도 남기지 않습니다.
68. 길이 보존 시험 ROM은 생성됐지만 선택 엔트리 첫 바이트 0x0209E0이 직전 엔트리와 bit 7 경계를 공유해 화면 target read 캡처가 `display-capture-artifact`에서 멈췄습니다. 패치 쓰기 경계는 그대로 두고 breakpoint만 엔트리 내부 0x0209E1로 옮겼으며, 목표 read가 확정되지 않았을 때 선택자 관측을 안전 캡처에 잘못 결합하지 않도록 했습니다.
69. 0x0209E0·0x0209E1은 같은 화면 경로에서 실제 read되지 않았고 B=147의 엔트리 서수 의미도 직접 증명되지 않았으므로 148번째 엔트리 가정을 철회했습니다. schema v5가 실제 read 0x44B1을 포함한다고 유일하게 계산한 23번째 엔트리(bit 1550..1750, 57심벌·200비트)를 새 시험 대상으로 선택합니다. 출력 없는 페이지 선택만 사용한 `한다`는 정확히 200비트 `EA4A95D4A95DB82BAF6FE63DBF98F6FE63DBF98F6FE6289688`로 왕복하며, 화면 breakpoint는 기존에 실제 관측된 0x44B1을 그대로 사용합니다. 로컬 상세 JSON 기록 실패는 안전 캡처 게시를 막지 않도록 격리했습니다.
70. 실패 상태의 최신 화면 캡처가 앞서 확정한 그룹 경계를 덮어쓰고 이전 로컬 시험 ROM을 재사용할 수 있는 경로를 차단했습니다. 확정 캡처를 `analysis/evidence/v5_1_confirmed_group_capture.json`에 불변 입력으로 고정하고, S25U는 매 시험 전에 이전 시험 ROM·IPS·빌드 보고서를 지운 뒤 새 ROM과 보고서가 생성됐을 때만 화면 캡처를 시작합니다.
71. 고정된 23번째 그룹 엔트리 전체를 정확히 200비트 `한다`로 바꾼 새 시험 ROM(SHA-256 `e263a1ec32e934e45daabbb7abf5b00f16033ae47ffbd137a528d280cd87227b`)은 0x44B1 read를 재현하지 못했습니다. 따라서 그룹의 바이트 겹침은 표시 엔트리 선택 증거가 아니며, 실제 런타임 시작점 0x44B1에서 왕복 확인된 87비트 스트림을 직접 사용합니다. 새 시험 문구는 정확히 87비트 `EA4A95D4A95D539D512D10`이고 bit 87 뒤 이웃 비트를 보존합니다.

## 현재 체크포인트

- 0x000B7A..0x000C2E 통합 범위 히트 뒤 실제 압축 대상 주소를 읽을 때의 매퍼 뱅크
- 한국어 문맥 0x00..0x20과 제어 심벌의 화면 의미
- And so began Mishaela's new ambition... 및 기존 한국어 문장의 정확한 엔트리 ID

조회표 없이 압축 뱅크의 모든 바이트를 시작점으로 읽는 방식은 거짓 양성이 많아 사용하지 않습니다.

## S25U 명령

~~~sh
cd ~/storage/shared/ShiningForceKR
git pull --ff-only
python tools/run_mobile_pipeline.py --rom "/storage/emulated/0/ROM/Shining Force Gaiden - Final Conflict (Japan).gg"
~~~

이 명령은 ROM과 보고서를 S25U 로컬의 build/ 및 reports/에만 생성합니다. 내 파일에서 ShiningForceKR/reports/NEXT_STEP.txt를 먼저 확인합니다. 원본 ROM은 덮어쓰지 않으며 Git 대상에서 제외됩니다.

USB ADB 최초 설정과 1회 실행 확인 뒤 케이블 없이 새 GitHub 커밋을
자동 처리하려면 `tools/manage_s25u_autopilot.sh install`을 사용합니다.
상태와 로그는 Termux 전용 `~/.local/state/shiningforcekr/`에 남고,
사용자용 상태 요약만 로컬 `reports/AUTOPILOT_STATUS.txt`에 생성됩니다.
ROM 경로와 상태 파일은 Git에 게시하지 않습니다.

캡처 없이 안전 요약을 GitHub에 공유할 때는
`--publish-safe-observation`을 붙입니다. 이 옵션은
`analysis/device/v5_1_latest_observation.json`만 검증·커밋·푸시하며,
ROM 바이트, 대사와 로컬 경로는 스키마에 포함할 수 없습니다.

## 다음 순서

1. 완료: S25U v5 전수 집계에서 0x000B7B가 58/60 종료·60/60 고유 대상으로 1바이트 시프트 대안보다 우세함을 기록했습니다.
2. 완료: 공식 Gearsystem 3.9.14 ARM64를 S25U 격리 환경에 설치했습니다.
3. 완료: 물리 뱅크 0·6·16의 정적 상위 조회표 후보를 slot 0/1/2에서 순차 추적했으나 현재 입력 구간의 뱅크 일치 read는 0회였습니다.
4. 완료: 동기화된 무입력 attract intro에서 0x33FA 디코더 진입과 bank 8 source-region read를 확인했습니다.
5. 완료: source-region 표본을 8개로 제한한 후 최대 1,024개 read를 따라가 한국어 Huffman 벡터·트리 및 bank 0x20 전환을 수집했습니다.
6. 각 read의 고정 operand-kind를 수집하고 자동 resolver로 압축 stream·벡터·트리 경계를 정확히 연결합니다.
7. 확정 후보가 가리키는 압축 대상 주소에 두 번째 read breakpoint를 걸고 실제 매퍼 뱅크가 포인터의 뱅크와 일치할 때만 확정합니다.
8. 히트 PC와 0x87000 런타임의 호출·데이터 흐름을 연결해 조회표 전체를 확정합니다.
9. 완료: 첫 화면 경로 시험 표식 `한다`를 Galmuri7과 픽셀 일치하는 페이지 6의 `한`·`다`로 교정하고 31비트 Huffman 왕복을 ROM 없이 검증했습니다.
10. 준비 완료: 런타임 확정 엔트리에만 적용되는 무변경 왕복·Expected Writes·IPS 재적용 빌더를 연결했습니다.
11. 자동 재검증 완료: B=147의 148번째 엔트리, 0x44B1 포함 23번째 그룹 엔트리, 0x44B1 독립 스트림 해석은 모두 목표 `한다`를 표시하지 못해 제외했습니다. B+1=148개 고정 출력 심벌 가설도 실제 관측 read 0x44B1에 도달하기 전에 디코드가 끝나 정적 안전검사에서 기각했습니다.
12. 완료: 0x33FA 진입에서 DE=2인 호출의 ROM 바이트 없는 64명령 레지스터 추적을 확보했습니다. `INC B`는 B=147을 148로 만든 뒤 `DJNZ` 진입용이며, 반복 본문은 `E=(HL); HL++; HL+=DE`로 길이 접두 레코드를 147개 건너뜁니다. 0x44B1은 payload가 아니라 이 루프가 읽은 길이 바이트였습니다.
13. 자동 재검증 중: 0x43DE부터 147개의 `1+length` 레코드를 전체 ROM에서 다시 순회하고, 다음 레코드의 길이 바이트를 보존한 payload만 Huffman 무변경 왕복·Expected Write·IPS 재적용 검사 뒤 `한다`로 교체합니다.
14. 완료: 정확한 0x4913 레코드 선택 뒤 가장 바깥 호출 복귀까지의 제한 trace는 7개 명령과 VDP 출력 0회를 확인했습니다. 첫 프레임 제한 trace에서는 12,371개 엔트리, 한국어 렌더러 진입 1회, 렌더러 내부 VDP 제어 출력 800회와 프레임 데이터 출력 192회를 확인했습니다. 다음 자동 실행은 원시 값은 로컬에만 보존하고, 렌더러 제어 출력 뒤의 VDP 데이터 출력만 순서가 이어진 소비 경로로 승인합니다.
12. 목표 인트로 문장과 기존 한국어 문장을 정확한 엔트리 ID로 재현합니다.
13. 콜드 부팅 기준으로 `한다` 자형과 SFKR-001부터 SFKR-007까지 에뮬레이터 검증합니다.

## 금지 사항

- 원본 ROM 덮어쓰기
- ROM 또는 미리 패치된 ROM 커밋
- 외부 영어 IPS 커밋
- 조회표와 사전이 불명확한 상태에서 대량 번역 삽입
- 정적 분석만으로 화면 문제 해결 선언
